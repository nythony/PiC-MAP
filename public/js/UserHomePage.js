var username = JSON.stringify(user.userid)
var project = JSON.stringify(user.project)
var task = JSON.stringify(user.task)

// client.query('SELECT "ProjectName" FROM "Project";', (error, results) => {
//     for (let row of results.row) {
//         if (row[""])
//     }
// }